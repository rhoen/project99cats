<!DOCTYPE html>
<html>
<head>
  <title>Project99cats</title>
</head>
<body>



<% if current_user %>
  <span>
    Current User: <%= current_user.user_name %>
  </span>
  <%= button_to "Log Out", session_url, method: :delete %>
<% else %>
  <a href="<%= new_session_url %>">Log in here</a>
<% end %>

<%= yield %>

</body>
</html>
